注意分开需求文档（只描述"要做什么"一用户能看到什么、系统能做什么，面向产品和业务。不该出现API
  URL、数据库字段这些实现细节。）和技术设计文档（描述"怎么做"一-接口设计、数据模型、架构方案，面向开发）。
  帮我设计以下功能，有不完善的地方后续讨论补充：
  1.中英文语言切换，目前前后端页面都是英文，需要切换语言功能。
  2.目前项目有账号注册功能，但没有要求填写邮箱，需要加上邮箱填写和验证码发送填写，做好前后端相关校验。
  3.邮件发送功能，我想使用我自己的163邮箱，实现用户注册时向用户邮箱发送验证码，用户输入验证码才可以注册账号。但目前项目有一个账号激活的流程，新用户注册后 is_active =
  False，看看这部分怎么处理，我设想填写了用户名、邮箱、密码、确认密码、验证码后，注册成功的账号就是正常使用的状态了。
  4.主业务，实现任务看板功能，具体实现后续进行详细讨论，我有一些大概想法
  【
  任务新增：创建任务（标题、描述、优先级、截止日期）
  任务编辑：修改任务内容、状态（待办 / 进行中 / 已完成）
  任务删除：逻辑删除任务，支持恢复
  任务列表：看板视图展示所有任务，支持状态筛选
  任务归属：任务仅归属创建用户，管理员可查看全部
  】
  5.完善一个轻量的RBAC权限，区分管理员/普通用户，根据项目中已有的逻辑来后续进行详细讨论。
  6.本地文件管理
  实现任务附件上传功能
  附件下载 / 删除：仅上传者可操作
  7.数据可视化看板：使用 Recharts 展示任务完成率、每日任务统计
  8.消息通知：任务截止提醒、评论提醒
  9.任务评论：对单条任务添加评论

  非功能需求：
  1. 性能需求
  任务列表 / 看板加载流畅，无明显卡顿
  接口响应时间 ≤ 300ms
  支持分页加载，避免大数据量卡顿
  2. 安全需求
  用户密码加密存储，禁止明文保存
  细粒度权限控制，禁止越权访问他人任务 / 数据
  本地文件上传做格式 / 大小限制，防止恶意文件

  我暂时想到的就这些