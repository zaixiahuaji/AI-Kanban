# admin 应用专用环境变量（覆盖 .env.frontend.prod）
#
# admin 运行在 3001 端口，与 web(3000) 的 NEXTAUTH_URL 不同。
# 本文件只覆盖 NEXTAUTH_URL，其余配置（API_URL、NEXTAUTH_SECRET、
# NEXT_PUBLIC_API_URL 等）均继承 .env.frontend.prod。

NEXTAUTH_URL=http://你的服务器公网IP:3001/api/auth
